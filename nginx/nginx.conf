upstream client {
	 server client:5556;
}

upstream api {
	 server api:5555;
}

server {
       listen 80;

       location / {
       		proxy_pass http://client;
       }
}

server {
       listen 5555;

       location / {
       		proxy_pass http://api;
       }
}